import type { CodegenConfig } from '@graphql-codegen/cli';

const config: CodegenConfig = {
  schema: [
    // Remove the leftover comments about Contentful.
  ],
  generates: {
    // Remove the leftover comments about Contentful.
  },
};
export default config;
